const Linestyle = ({ color }) => (
    <hr
        style={{
        	color: color,
            backgroundColor: color,
            marginLeft: '1%',
            width: '98%',
            opacity: '0.1',
        }}
    />
);

export default Linestyle;